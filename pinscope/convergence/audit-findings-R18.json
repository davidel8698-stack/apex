{
  "round": 18,
  "generated_at": "2026-05-22T13:20:00Z",
  "findings": [],
  "investigation_findings": [
    {
      "id": "F-18-01",
      "axis": "8-data-schemas / 11-integration-surface (§8.6, §9.4, §10-C)",
      "confidence": "CONFIRMED",
      "severity": "P2",
      "current_state": "The §10-C operation flow appends to the shared `command.history` HistoryManager TWICE per operation, and persists `.pinscope/history.json` with a lossy, lagging payload. CommandBar's Enter handler (CommandBar.tsx:111-125) appends a placeholder entry `{ parsed: null, result: 'sent' }` then synchronously calls `persistHistory(history.list())` (CommandBar.tsx:121) which POSTs the full list to `/__pinscope/history`. It then calls `onSubmit` -> PinScope.onSubmit -> `command.bridge.send` (PinScope.tsx:208) -> `ClaudeBridge.send` appends a SECOND entry `{ parsed: <Operation>, result: 'sent' }` to the SAME `command.history` (ClaudeBridge.ts:35). ClaudeBridge never calls `persistHistory` and PinScope.tsx never re-POSTs after a send. Live trace of `<PinScope/>`: ONE operation submit -> 1 history POST, persisted file has 1 entry with `parsed: null` (the real Operation is in-memory only). TWO submits -> 2 POSTs, last persisted file = 3 entries `[parsed:null, parsed:op:sent, parsed:null]`.",
      "gap": "§9.4 defines a history entry's `parsed` field as the parsed Operation; §10-C ('Operation via CommandBar ... clipboard + history') and §8.6 ('History persisted to .pinscope/history.json') require the persisted history to record the operation. Two defects: (1) DUPLICATION — every operation produces two `command.history` entries (a `parsed:null` CommandBar placeholder plus the `parsed:<Operation>` ClaudeBridge entry), so `.pinscope/history.json` carries doubled rows. (2) LOSS / LAG — ClaudeBridge's real `parsed:<Operation>` entry is never persisted at the time it is created; it reaches disk only if a LATER CommandBar submit happens to trigger another `persistHistory`. The most recent operation's parsed record never lands on disk, and a session's final operation is permanently unpersisted. The persisted file is therefore both duplicated and lossy. AC-053 verifies `ClaudeBridge.send` in ISOLATION (claude-bridge.test.ts:77 asserts `entries.length toBe(1)` with a directly-constructed manager) and AC-054 only checks autocomplete latency — neither exercises the persisted-file content of the full CommandBar -> onSubmit -> ClaudeBridge path, so the gap is invisible to STEP 1-4.",
      "re_read": "Live trace: a temp test `tests/unit/runtime/__trace2.test.tsx` rendered the real `<PinScope/>`, stubbed `fetch`, submitted `e_7.bg → red` then `e_7.padding → 8px` via the CommandBar, captured all `/__pinscope/history` POST bodies -> `historyPosts=2 lastPersistedEntries=3 kinds=[null,op:sent,null]` (temp file deleted after the run). Static cross-check: `grep -n 'persistHistory' src/runtime/components/CommandBar.tsx` -> defined L44, called ONLY at L121 (CommandBar Enter handler). `grep -rn 'persistHistory' src` -> no call site outside CommandBar.tsx. `grep -n 'history.append' src/runtime/managers/ClaudeBridge.ts` -> L35 (`parsed: operation, result: 'sent'`) and L43 (`result: 'failed'`); ClaudeBridge has no `fetch`/`persistHistory` and PinScope.tsx onSubmit (L172-217) never re-POSTs history after `bridge.send`. CommandBar.tsx:114-120 builds the placeholder entry with `parsed: null`."
    }
  ],
  "blocked": [
    { "ac": "AC-023", "phase": "P1", "severity": "P0", "reason": "verify env 'browser' unavailable (env-capabilities.browser=false); Playwright ::before content read needs a browser engine.", "verdict_source": "ac-results-R18.json: UNAVAILABLE" },
    { "ac": "AC-030", "phase": "P1", "severity": "P1", "reason": "verify env 'browser' unavailable; Playwright hover/InfoPanel assertion needs a browser engine.", "verdict_source": "ac-results-R18.json: UNAVAILABLE" },
    { "ac": "AC-061", "phase": "P4", "severity": "P3", "reason": "verify env 'browser' unavailable; cross-origin iframe needs two real origins (matrix kind=manual).", "verdict_source": "ac-results-R18.json: UNAVAILABLE" },
    { "ac": "AC-063", "phase": "P4", "severity": "P3", "reason": "verify env 'browser' unavailable; @media print rendering needs a browser print engine (matrix kind=manual).", "verdict_source": "ac-results-R18.json: UNAVAILABLE" },
    { "ac": "AC-082", "phase": "P2", "severity": "P1", "reason": "verify env 'browser' unavailable; Playwright integration suite needs a browser binary (matrix kind=manual).", "verdict_source": "ac-results-R18.json: UNAVAILABLE" },
    { "ac": "AC-083", "phase": "P4", "severity": "P3", "reason": "verify env 'browser' unavailable; visual-regression screenshots need a browser (matrix kind=manual).", "verdict_source": "ac-results-R18.json: UNAVAILABLE" },
    { "ac": "AC-106", "phase": "P5", "severity": "P2", "reason": "verify env 'apex-install' unavailable (env-capabilities.apex_install=false); /apex:health-check needs APEX synced to ~/.claude/ (matrix kind=manual).", "verdict_source": "ac-results-R18.json: UNAVAILABLE" }
  ],
  "coverage": {
    "axes_swept": [
      "1-build-pipeline (AST data-pin injection, getElementName, excludeTags/filePattern/excludePattern gates)",
      "2-pin-id-stability (file:line:column stableKey, getOrAssign monotonic + never-reused, reconcile soft-delete)",
      "3-production-zero (vite-react dist grep = 0 PinScope bytes; transformIndexHtml strip; AC-010/074 PASS)",
      "4-runtime-isolation (createPortal to document.body, data-pinscope-ui=root wrapper, z-index, prod NODE_ENV null guard)",
      "5-inspection-layer (useHoveredElement HUD-escape walk, InfoPanel, PinBadges, TopBar live state-override)",
      "6-measurement-snapshot (MeasurementTool render, SnapshotManager/EndpointSnapshotStore persist + flush error surfacing)",
      "7-operation-protocol (operation-parser grammar — all §11 forms + typed errors, select-command canonical-manager routing)",
      "8-data-schemas (PinMap §9.1 / Snapshot §9.2 / Operation §9.3 / History §9.4 — History persist path traced)",
      "9-edge-cases (§12 set; swallowed-error hunt across catch blocks)",
      "10-performance-budgets (§13; machine AC-070..076 PASS)",
      "11-integration-surface (src/index.ts public API, Vite/Next/Webpack entry points, §10 flow seams B/C/D)",
      "12-phase-dod (§16 DoD items P1-P5; §17 APEX integration)"
    ],
    "sections_reviewed": [
      "SPEC §6 (build-time module)", "SPEC §7 (runtime module / PinScope root)",
      "SPEC §8 (component specs §8.1-§8.11)", "SPEC §9 (data schemas)",
      "SPEC §10 (behavioral flows A-E)", "SPEC §11 (operation protocol)",
      "SPEC §12 (edge cases)", "SPEC §13 (performance)", "SPEC §14 (testing)",
      "SPEC §15 (deployment)", "SPEC §16 (phase DoD)", "SPEC §17 (APEX integration)",
      "Appendix A (69 ACs)", "Appendix B (loop contract)"
    ],
    "carry_over_investigated": {
      "subject": "VERIFY-R17 mutation survivor M1 — `or-to-and` at src/runtime/hooks/useSelectedElement.ts:24 (`if (!pinId || typeof document === 'undefined') return null;` inside `resolveSelected`).",
      "verdict": "BENIGN coverage margin — NOT an off-matrix finding.",
      "detail": "`resolveSelected(pinId)` returns a `HoveredElement | null` view of the pin id the SelectionManager holds. The original `||` guard is correct: bail out (return null) if EITHER the pin id is falsy OR there is no `document`. The mutant `&&` only diverges from `||` when exactly one operand is true. Under jsdom/happy-dom `typeof document` is never `'undefined'`, so the second operand is always false; the predicate reduces to `!pinId` (original) vs constant-false (mutant). When the operand split matters — a null `pinId` in a defined-document env (reachable: the lazy `useState` initializer at useSelectedElement.ts:46 calls `resolveSelected(manager.selectedPin)`, and `selectedPin` is null on a fresh mount with no `#select=` hash) — the original returns `null` at L24; the mutant falls through to `document.querySelector('[data-pin=\"null\"]')` at L25, which yields no element, so the `!(el instanceof HTMLElement)` guard at L26 returns `null` anyway. Both paths produce an identical `null` result for every reachable input; the mutant is behaviorally equivalent on this tree. No behavioral gap, no dead path, no swallowed failure — consistent with the VERIFY-R17 'non-blocking coverage finding' adjudication. The R18 recommendation to add a `resolveSelected(null)`-returns-null test would kill the mutant but closes a coverage hole only, not a defect.",
      "re_read": "Read src/runtime/hooks/useSelectedElement.ts L22-28 (`resolveSelected`: L24 `||` guard, L25 `document.querySelector`, L26 `instanceof HTMLElement` guard) and L46-48 (`useState` initializer calls `resolveSelected(manager.selectedPin)`). Cross-checked SelectionManager.ts L8-9,L16-18 (`selected` defaults `null`, `selectedPin` getter) and L59-66 (`restoreFromHash` — `selected` stays null with no matching `#select=` hash). The mutated `&&` predicate and the original `||` predicate yield the same `null` return for every reachable `(pinId, document)` pair."
    }
  },
  "summary": {
    "open": 1,
    "blocked": 7,
    "regressions": 0,
    "confirmed": 1,
    "suspected": 0
  }
}
