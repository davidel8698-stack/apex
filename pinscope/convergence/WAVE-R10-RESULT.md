# Wave Results — PS-R10

Wave **W1 PASS**. (The plan's W2 — `STATUS.md` reclassification — is not a
separate executor wave: `STATUS.md` is generated by `render-status.mjs` at
close, so the reclassification lands mechanically in STEP 7.)

## W1 — R-061 + R-083

Both R-items are self-contained and touch fully disjoint file sets, so they
ran concurrently in one wave (write-serial-safe — every file has exactly one
owner).

### R-061 — Cross-origin iframe outline + label overlay (AC-061)
- **Created** `src/runtime/utils/iframe-overlay.ts` — mirrors the sibling
  `shadow-dom.ts`. Exports `IFRAME_ATTR` (`data-pin-iframe`),
  `IFRAME_OVERLAY_ATTR` (`data-pinscope-iframe-overlay`), `isCrossOriginFrame`
  (try/catch over `contentDocument` — throw or `null` ⇒ cross-origin),
  `markCrossOriginFrames` (sweeps `<iframe>`s, draws an absolute-positioned
  dashed-outline overlay with a label, `pointer-events: none`, `z-index`
  reused from `constants.ts` `Z_SELECTED`, no injection), and `isIframeLimited`.
- **Created** `tests/unit/runtime/iframe-overlay.test.ts` — jsdom/Vitest,
  `describe('Cross-origin iframe overlay (AC-061)')`, 8 tests.

### R-083 — Playwright visual-regression suite (AC-083)
- **Created** `tests/integration/visual-regression.spec.ts` —
  `test.describe('PinScope visual regression (AC-083)')`, the SPEC §14 matrix
  (4 viewports × light/dark × host-CSS on/off → 16 tests, baseline + InfoPanel
  screenshots) via `toHaveScreenshot`.
- **Modified** `playwright.config.ts` — added a `projects` array
  (`chromium`/`firefox`/`webkit`) and an `expect.toHaveScreenshot` tuning
  block. `testDir`, `use.baseURL`, and the `webServer` block preserved verbatim.

## W1 verification

| Check | Command | Result |
|-------|---------|--------|
| Strict typecheck | `npm run typecheck` | exit 0 — clean |
| New R-061 suite | `npx vitest run tests/unit/runtime/iframe-overlay.test.ts` | **8/8 pass** |
| Full unit suite | `npx vitest run` | 27 files, **257 tests pass** |
| Visual-regression spec | `tsc --noEmit` | type-checks (not run — no browser binary) |

No regression: all 249 PS-R1–R9 tests still green. New (8): cross-origin
iframe overlay. Guarded files (`shadow-dom.ts`, `edge-cases.test.ts`,
`constants.ts`, `pinscope.spec.ts`, `vitest.config.ts`) confirmed byte-unchanged.

## Wave-AC re-verification

`ac-verify.mjs --round 10` re-run for the wave's ACs: AC-061 and AC-083 are
`manual`-kind, `env: browser`. They remain `UNAVAILABLE` here (no browser
binary) — **expected and correct**. The wave's purpose was to make them
*correctly* `BLOCKED` — implementation present + test artifact authored —
rather than `BLOCKED` masking an absent deliverable. The headless-verifiable
portion of R-061 (cross-origin detection + overlay DOM construction) is proven
green by the 8 new jsdom tests; the browser-only residual (visual correctness
against a real second origin) keeps AC-061 env-gated. AC-083's suite is
authored and type-checks; it executes on a browser-capable CI.
